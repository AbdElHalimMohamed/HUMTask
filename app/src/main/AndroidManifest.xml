<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.halim.humtask">
	<application android:allowBackup="true"
	             android:name=".App"
	             android:label="@string/app_name"
	             android:icon="@mipmap/ic_launcher"
	             android:roundIcon="@mipmap/ic_launcher_round"
	             android:supportsRtl="true"
	             android:theme="@style/AppTheme">

		<activity android:name=".ui.activity.QuestionListActivity">

			<intent-filter>

				<category android:name="android.intent.category.LAUNCHER"/>
				<action android:name="android.intent.action.MAIN"/>

			</intent-filter>

		</activity>
		<activity android:name=".ui.activity.AddQuestionActivity"/>
		<activity android:name=".ui.activity.QuestionDetailsActivity"/>
		<activity android:name=".ui.activity.AddAnswerActivity"/>
		<activity android:name=".ui.activity.AnswerDetailsActivity"/>

	</application>
</manifest>
